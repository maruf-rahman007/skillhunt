// next.config.mjs
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com'],
      domains: ['ui-avatars.com'], // Add your allowed domains here
    },
  };
  
  // Use export default to export the configuration
  export default nextConfig;
  